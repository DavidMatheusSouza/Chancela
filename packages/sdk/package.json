{
  "name": "@chancela/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Ask Chancela whether an AI agent may act, and check the answer yourself instead of trusting the server that gave it.",
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@chancela/shared": "workspace:*",
    "viem": "^2.21.32"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^2.1.3" }
}
